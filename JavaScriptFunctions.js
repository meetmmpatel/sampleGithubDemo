

//type of functions

function getName(params) {
        //void functions
}

function getArgs(args) {
        return args
        // this functions with return type
}

const someName = (firstName, lastName) => {
        return firstName + lastName;
}

